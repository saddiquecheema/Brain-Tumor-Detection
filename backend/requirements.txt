# ============================================================
#  Brain Tumor MRI Classification System
#  Python 3.10
#  Install: pip install -r requirements.txt
# ============================================================

# ── Deep Learning ─────────────────────────────────────────────
tensorflow                  # Uses whatever version is installed
# keras is bundled with tensorflow — no separate install needed

# ── Image Processing ──────────────────────────────────────────
opencv-python==4.9.0.80     # tumor region annotation (bounding box)
Pillow==10.3.0              # image open / convert / resize
numpy==1.26.4               # array operations

# ── Web Framework ─────────────────────────────────────────────
flask==3.1.0                # web server & routing
flask-cors==5.0.0           # cross-origin requests (needed for Vercel/ngrok)

# ── AI Chatbot — NeuroGuide ───────────────────────────────────
groq==0.9.0                 # Groq API — Llama 3.1 (free)
python-dotenv==1.0.1        # .env file se GROQ_API_KEY load karna
